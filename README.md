# Potatoland Frontend

React + Vite frontend for Potatoland.

## Security Notes

- Password validation checks length only: 8 to 100 characters.
- User-generated content must be rendered as plain text.
- `dangerouslySetInnerHTML` is forbidden by ESLint.

## Scripts

```powershell
npm install
npm run dev
```
